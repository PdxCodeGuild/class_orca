//alert("Hello World")

// var num1 // example to show no value popping up. "var" is declaring a variable
// console.log(num1) // is like a print statement for testing purposes

let a = 5; // number
let b = 10.4; // number
let c = "hello!"; // string
let d = true; // boolean
let e = null; // null
let f = undefined; // undefined

var num1 = 0  // "var" is not used as much any more (replaced by let)
let num2 = 1 // "let" should be used to declare the variable
const num3 = 2

// arrays are like python lists
let fruits = ["apple", "bananana", "pear"];
fruits[0] = 'cherry'; // set the element at that position
fruits.push('pomegranate'); // add a new element

// objects are like Python dictionaries
let person = {
    firstName:"John",
    lastName:"Doe",
    age:46
};
person.age += 1;
person['age'] += 1;

// alert(person.age + person.firstName + person.lastName)

console.log(person.age)







